<li>
  <%= avatar_for user, size: 72 %>
  <%= link_to user.username, user %>
  <% if user.admin? && !current_user?(user) %>
    | <%= link_to "Delete User", user, method: :delete,
                                  data: { confirm: "Really delete this fool?" } %>
  <% end %>
</li>